{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<style>
    .wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .leftbox { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    nav { width: auto; height: auto}
    a{height: 20px; width: 100px}
</style>

<div class="wrapper">
    <h1>Hello {{ User.username }}! ✅</h1>

    <div class="leftbox">
        <nav>
            mike is gay
            <a href="{{ path('docker') }}">docker</a> <br>
            <a href="{{ path('projects') }}">projects</a>
        </nav>
    </div>
    <a href="{{ path('logout') }}">Logout</a>
</div>
{% endblock %}
